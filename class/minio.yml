parameters:
  kapitan:
    compile:
      - input_paths:
          - minio/component/app.jsonnet
        input_type: jsonnet
        output_path: apps/
      - input_paths:
          - minio/component/main.jsonnet
        input_type: jsonnet
        output_path: minio/

