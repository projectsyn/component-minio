apiVersion: v1
kind: ServiceAccount
metadata:
  labels:
    app: minio-update-prometheus-secret
    chart: minio-8.0.0
    heritage: Helm
    release: minio-minio
  name: minio-minio-update-prometheus-secret
